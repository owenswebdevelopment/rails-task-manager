<div class="container">

  <h1><%= @task.title %></h1>
   <h2><%= @task.details %></h2>
   <p><%= @task.completed %></p>

</div>
 <%=link_to "back", tasks_path, class: "btn btn-primary"%>
  <%=link_to "edit", edit_tasks_path(@task), class: "btn btn-primary"%>
   <%# <%=link_to "delete", tasks_path(@tasks), class: "btn btn-primary"%>

<%= link_to 'Delete', task_path(@task), class: 'btn btn-danger',
 data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }  %>
